cca

pop=330e3;
v_vac=zeros(pop,1);  % percent of bin vaccinated
v_ages=zeros(pop,1); % avg age per bin
v_eff=zeros(pop,1);  % avg efficiency per bin
v_tvac=zeros(pop,1); % 

vpd=1000;

nday=100;

for d=1:nday
    
end